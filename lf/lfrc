# Appereance
set drawbox
set icons
set ifs "\n"

$lf -remote "send $id set previewer ${XDG_CONFIG_HOME:-$HOME/.config}/lf/scope"

# Remappings
map m push %mkdir<space>
map <c-r> reload
map <c-h> set hidden!
map D delete


